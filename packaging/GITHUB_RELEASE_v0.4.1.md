# Chuni-Eventer v0.4.1

## 更新内容

- 窗口标题显示版本号（`Chuni Eventer v0.4.1`）
- 角色列表支持右键删除角色（移除对应 `chara/` 与 `ddsImage/` 目录）
- PNG→BC3 DDS（新增角色立绘、更换乐曲封面等）改为后台编码并显示进度，缓解打包版界面长时间无响应

## 构建说明

与 v0.4.0 相同：需先准备 `Foahh/PenguinTools` 再构建 `PenguinBridge`（见 `packaging/BUILD_AND_DISTRIBUTION.md`）。
