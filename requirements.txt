# ASCII-only: pip on Windows (cp936) decodes requirements as system encoding.
PyQt6>=6.10.0
PyQt6-Fluent-Widgets>=1.7.0
Pillow>=12.0.0
# quicktex: BC3 DDS encode/decode without compressonator (optional fallback in settings)
quicktex>=0.3.0
